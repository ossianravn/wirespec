{
  "name": "@wirespec/studio-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm --dir ../runtime run build && pnpm run build && node ./scripts/smoke-test.mjs"
  }
}
