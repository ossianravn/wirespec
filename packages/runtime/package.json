{
  "name": "@wirespec/runtime",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate:examples": "pnpm run build && node ./scripts/build-examples.mjs",
    "test": "pnpm run build && node ./scripts/smoke-test.mjs"
  }
}
